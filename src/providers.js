export const providers={gemini:{label:'Gemini',base:'https://generativelanguage.googleapis.com/v1beta'},grok:{label:'Grok',base:'https://api.x.ai/v1'},groq:{label:'Groq',base:'https://api.groq.com/openai/v1'},openrouter:{label:'OpenRouter',base:'https://openrouter.ai/api/v1'},cerebras:{label:'Cerebras',base:'https://api.cerebras.ai/v1'},sambanova:{label:'SambaNova',base:'https://api.sambanova.ai/v1'},huggingface:{label:'Hugging Face',base:'https://router.huggingface.co/v1'}};
export async function chooseModel(provider,key){const p=providers[provider];if(!p)throw Error('Choose an AI provider.');if(provider==='openrouter')return 'openrouter/free';const r=await fetch(p.base+'/models',{headers:provider==='gemini'?{'x-goog-api-key':key}:{Authorization:'Bearer '+key},signal:AbortSignal.timeout(15000)});if(!r.ok)throw Error('Could not validate this key with the provider. Check the key and account access.');const data=await r.json();const names=(provider==='gemini'?(data.models||[]).filter(m=>m.supportedGenerationMethods?.includes('generateContent')).map(m=>m.name.replace(/^models\//,'')):(data.data||[]).filter(m=>m.active!==false).map(m=>m.id)).filter(n=>!/(embed|whisper|tts|audio|vision-only|image|guard|moderation)/i.test(n));const preferred=names.find(n=>/flash-lite/i.test(n))||names.find(n=>/flash/i.test(n))||names.find(n=>/llama-3.3-70b|gpt-oss-120b|instruct|grok.*fast/i.test(n))||names[0];if(!preferred)throw Error('No supported writing model is available for this key.');return preferred;}
